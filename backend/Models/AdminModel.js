// class Admin {
//     constructor(username, email, password) {
//       this.username = username;
//       this.email = email;
//       this.password = password;
//       this.isAdmin = true; // You can customize this based on your authentication logic
//     }
  
//     // Example method to check if the user has admin privileges
//     checkAdminPrivileges() {
//       return this.isAdmin;
//     }
//   }
  
//   // Example usage:
//   const adminUser = new Admin('admin', 'admin@example.com', 'admin123');
  
//   // Checking if the user has admin privileges
//   if (adminUser.checkAdminPrivileges()) {
//     console.log(`${adminUser.username} has admin privileges.`);
//   } else {
//     console.log(`${adminUser.username} does not have admin privileges.`);
//   }
  